import React from "react";

interface Props {}

const Profilesaved = (props: Props) => {
  return (
    <div className="w-full tracking-widest h-40 md:h-80 flex justify-center items-center">
      Nothing saved
    </div>
  );
};

export default Profilesaved;
