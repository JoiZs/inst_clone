import React from "react";

interface Props {}

const Noactivity = (props: Props) => {
  return (
    <div className="w-full h-full flex justify-center items-center">
      No activity
    </div>
  );
};

export default Noactivity;
